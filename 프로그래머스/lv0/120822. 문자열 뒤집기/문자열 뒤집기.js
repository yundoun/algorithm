function solution(my_string) {
    var answer = my_string.split('')
    return (answer.reverse()).join('');
    
    // const tmp = tmp.join()  // "h,e,l,l,o"
    // const tmp2 = tmp.join(+) // "h+e+l+l+o"
}